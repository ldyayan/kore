>rand_prog 4~
  if inroom($r) == here
    if clan($r) == house vherin'ultrin
      if hitprcnt($r) < 100
        cast 'cure critic' $r
        mpsilent mpcallmagic 'cure critic' $r
      else
        if rand(10)
          mpechoat $r $I stares at you with its huge blank eyes.
          mpechoaround $r $I stares at $R blankly, waiting for something.
        endif
      endif
    endif
  endif
~
|
